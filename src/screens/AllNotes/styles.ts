import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  card: {
    marginBottom: 20,
  },
  body: {
    marginBottom: 5,
    color: 'gray',
  },
});

export default styles;
